---
name: create-readme
description: |-
  创建、重写和审校面向项目最终用户的 README.md。先完整检查仓库，再以读者任务为中心筛选内容，
  清除聊天背景、修改过程、无关否定、无端假设、预防式前提、无端转折、实现备忘和“技术正确但读者不需要”的信息。
  写完后必须读回成稿并改正功能异常句。用于新建 README、重写现有 README、公开仓库前整理项目首页，
  或修复 README 冗余、突兀和不自然的问题。

  示例:
  - user: "为这个项目创建 README" → 检查仓库后生成面向目标读者的项目首页
  - user: "重写 README，让它适合公开仓库" → 保留有用事实，清除开发过程和对话痕迹
  - user: "README 里废话太多，帮我精简" → 按读者价值逐段筛选，而非机械删词
  - user: "补充安装和使用说明" → 核验命令与配置后更新必要章节
---

# Create README

## 目标

- 生成结构清晰、信息充分、易于阅读且适合开源项目展示的 `README.md`。
- 先完整理解项目和 workspace，再决定写什么；调查可以全面，README 输出必须有选择。
- 让第一次接触项目的外部读者在没有看过聊天、提交历史、工作笔记或旧稿的情况下，独立判断项目用途并完成关键操作。
- “完整”表示覆盖读者完成主要任务所需的信息，不表示罗列所有已发现的技术事实。

## 核心原则

### 1. 把源材料与最终 README 分开

- 将用户消息、旧 README、代码审查结果、开发说明和调查发现视为源材料，不直接当作成稿。
- 从源材料中提取可验证的项目事实、读者任务和必要约束，再重新组织为独立可读的 README。
- 用户在对话中问过某件事，不代表 README 必须回答它；只有目标读者也需要该信息时才写入。
- 不把“为什么这样写”“这次改了什么”“之前哪里不对”等写作过程带入 README。

### 2. 全面调查，选择性输出

- 审查整个项目和 workspace，包括现有文档、入口、源码、依赖声明、配置、脚本、测试、示例和发布方式。
- 全面调查用于保证准确性，不用于扩大 README 篇幅。
- 不因发现某个实现细节就自动写入 README，也不因没有找到某项内容就主动声明项目不具备它。
- 无法从项目证据确认的功能、平台、性能、兼容性和使用场景不得猜测。

### 3. 以读者任务决定内容

起草前明确：

1. 主要读者是谁，例如最终用户、首次安装者、库调用者、部署人员或项目评估者。
2. 读者打开 README 要完成什么，例如判断是否适用、理解主要能力、完成最短成功路径、找到必要配置或避开重要风险。
3. 哪些深入内容应放入专用文档，而不是展开在 README 中。

README 常见职责包括：

- 说明项目是什么、解决什么问题。
- 展示已验证的主要能力和适用场景。
- 提供最短可运行或可使用路径。
- 说明关键配置、典型用法和会影响结果的重要限制。
- 引导读者前往更深入的 API、架构、部署或贡献文档。

README 通常不承担：

- 逐文件代码解释或完整架构档案。
- 开发日志、审查报告、修改说明或聊天答复。
- 与当前项目无关的通用技术科普。
- 只有维护者才关心的内部容错和实现细节。

## 内容准入门槛

每个标题、段落、列表项和提示都必须依次通过三道门槛。

### A. 证据门槛

- 内容由源码、配置、脚本、测试、已有项目文档或用户明确提供的项目事实支撑。
- 文件路径、命令、链接、配置名、环境变量、平台支持和版本要求均已核验。
- “未找到”不等于“确定不存在”。证据不足时继续调查、向用户确认或省略。
- 不虚构功能、性能结果、兼容性、路线图或使用场景。

### B. 读者价值门槛

内容至少满足一项：

- 帮助目标读者判断项目是否适合自己。
- 帮助读者完成安装、运行、配置或典型操作。
- 解释完成操作所需的关键概念。
- 防止高概率或高后果的真实错误。
- 说明会实际改变使用结果的重要边界。

仅仅“技术上正确”不是入选理由。对每段执行删除测试：

> 删除后，目标读者是否更难作出决定、完成操作或避开实际风险？

如果答案是否定的，删除、压缩或移出 README。

### C. README 位置门槛

- 首次采用和首次使用所需的信息放入 README。
- 完整 API、深入架构、复杂部署、贡献流程和维护细节放入专用文档，并在必要时提供链接。
- 内部实现细节只有在直接影响用户操作或结果时才保留。
- 不创建 `LICENSE`、`CONTRIBUTING`、`CHANGELOG` 等专用文件已有职责的重复章节。

## 病句功能分类

不要依靠禁用词表。判断一句话在文档中的实际功能。换词但保留同样功能，仍然不合格。

对每一句先问：

> 这句话在消除什么误解？这个误解读者会从哪来？

答不出读者来源，就删。

下列功能默认清除。

- **过程泄漏**：对话回应、修改说明、调查过程、作者备忘、内部 TODO、审阅口吻。
- **无源否定**：否认一个文中未建立、读者也无理由期待的对象。典型细句：`无需额外配置`、`无需安装第三方依赖`、`并不是一个框架`、`这不会影响现有数据`、`没有 requirements.txt`。安装和运行章节要直接写环境和命令，禁止用“无需安装 X / 没有某文件”代替正面要求。
- **无端假设**：引入当前读者任务里不存在的身份、环境或情景。典型细句：`如果你已经在生产环境……`、`大多数用户可能已经……`。
- **预防式前提**：为尚未出现的误解做铺垫、提醒或声明。典型细句：`在开始之前请确保`、`为避免混淆`、`需要注意的是`、`值得一提的是`。
- **无端转折**：先造一个文中不存在的对立面再推翻。典型细句：`虽然本项目很简单，但是`、`与传统方案不同`、`这不是 X，而是 Y`。
- **防御性辩解**：为技术选型、缺失文件或项目定位回应一个 README 中从未出现的质疑。
- **事实堆积**：把内部容错、低层实现、未来兼容和边缘行为写入 README，只因为它们存在。
- **空泛宣传**：`强大、现代、革命性、无缝、易于使用` 等无法帮助读者判断的套话。
- **凑章节内容**：为了模板完整而解释项目不存在的部署方式、接口、依赖或文件。

定位线索（只用来找句子，不是准许这么写）：`无需`、`不会`、`注意`、`然而`、`虽然`、`如果`、`不是`、`而是`、`但是`。命中后必须做语义判断；词面本身不是删除理由。成稿默认不要用 `不是…而是…`、`但是` 这类对比句；有差异就直接写实际行为和后果。

### 否定信息的合法条件

否定句并非一律删除。只有同时满足以下条件时才保留：

1. 读者有合理理由预期相反行为。
2. 该差异会影响安装、数据、安全、兼容性或采用决策。
3. 项目证据足以支持该结论。
4. 文中说明了具体后果或可执行的替代方案。

优先正面陈述项目实际做法。不要为了显得全面而主动列举项目不做什么。

## 正反例

示例用于说明判断方法，不是固定句式模板。

### 对话与过程隔离

- 反例：`按你的要求，我们补充了 Windows 安装方式。`
- 正例：`Windows：运行官方 Python 安装程序并启用 Tcl/Tk 组件。`

- 反例：`检查项目后发现它使用 SQLite。`
- 正例：`项目使用 SQLite 存储本地数据。`

### 无源否定与重要限制

- 反例：`无需额外安装依赖，也没有 requirements.txt。`
- 正例：`Python 3.10+。运行：python hashdir.py <目录>`
- 理由：读者要的是怎么运行。不要解释某个文件为什么不存在。

- 反例：`这并不是一个 Web 框架，也不会作为服务长期运行。`
- 正例：省略，或只写它实际是什么、怎么调用。
- 理由：读者若没把 CLI 当成框架，这句只在消除一个你自己立的误解。

- 合理：`数据只保存在内存中，进程重启后会丢失。`
- 合理：`服务不提供身份认证；暴露到公网前请配置反向代理认证。`

### 无端假设、预防式前提、无端转折

- 反例：`如果你已经在生产环境部署过类似工具，可以直接跳过安装。`
- 正例：省略。会跳过的人自己会跳过。

- 反例：`在开始之前，请确保已安装 Python。为避免混淆，下面只介绍一种安装方式。`
- 正例：`运行：python hashdir.py ./photos`

- 反例：`虽然本工具很简单，但与传统 GUI 方案不同，它直接在命令行完成。`
- 正例：`传入目录即可得到每个文件的 SHA-256。`

### 粒度与位置

- 反例：在 README 中逐个解释内部类、私有函数和完整调用链。
- 正例：概述用户可见能力，并链接到架构或 API 文档。

## 工作流

### 1. 检查项目

- 阅读整个项目和 workspace 中与用途、功能、运行、配置和使用有关的材料。
- 确认项目类型，例如 CLI、库、桌面应用、Web 服务或模板仓库。
- 核验所有准备展示的命令、路径、配置和示例。
- 找到真实存在且适合展示的 logo 或 icon 时，将其用于 README 页头。

### 2. 建立 README 契约

- 写明主要读者、首要任务、最短成功路径和 README 边界。
- 根据项目形态选择章节，不把所有项目强行套进同一模板。
- 先列候选内容，再通过三道准入门槛筛选。

### 3. 组织内容

优先采用渐进披露：

1. 项目是什么、解决什么问题。
2. 已验证的主要能力。
3. 最短成功路径。
4. 典型用法。
5. 必要配置和关键限制。
6. 指向深入文档的链接。

只创建项目实际需要的章节。避免空章节、占位内容和为“完整”而加入的无关说明。

### 4. 起草 README

- 使用 GFM（GitHub Flavored Markdown）。
- 仅在确实改善重要提示或风险表达时使用 GitHub admonition。
- 克制使用 emoji，保持简洁直接。
- 使用具体、可验证的能力描述，避免宣传套话。
- 让标题、命令、示例和术语前后一致。

可参考以下 README 的结构、语气和表达方式，但不能复制其项目事实，也不能强制套用其结构：

- https://raw.githubusercontent.com/Azure-Samples/serverless-chat-langchainjs/refs/heads/main/README.md
- https://raw.githubusercontent.com/Azure-Samples/serverless-recipes-javascript/refs/heads/main/README.md
- https://raw.githubusercontent.com/sinedied/run-on-output/refs/heads/main/README.md
- https://raw.githubusercontent.com/sinedied/smoke/refs/heads/main/README.md

### 5. 写后审计（阻断交付）

起草写入文件后，文档不算完成。必须另开一轮，只删改，不扩写。

1. 用 Read 把成稿再读一遍。没读回文件，不得交付。
2. 按句扫描「病句功能分类」。每处只能：删 / 改成正面事实 / 保留。保留必须指出读者会从哪产生相反预期。
3. 扫描到的功能异常句必须当场改回文件，然后再 Read 一轮核对文件已经是改后文本。只在回复里写「改写」「删除」、只在心里改、把修改留给别人，都算未完成。审计结论必须和文件一致。
4. 假设读者没有看过聊天、旧 README、提交记录或作者笔记，逐段回答：这段服务谁、回答什么真实问题、依据哪条项目事实、删除后会损害哪项决定或操作、为什么应放在 README。答不出的标题、段落或列表项必须删除、压缩或移出。

不要把读回过程、抽查记录或修改说明写入 README。

## 交付门槛

交付前必须全部满足：

### 事实

- 项目特定事实均可追溯到已检查的代码、配置、测试或文档。
- 路径、链接、命令、配置名、环境变量和平台声明已核验。
- 未把“没有找到”写成“确定不存在”。
- 没有猜测功能、兼容性、性能、路线图或使用场景。

### 读者价值

- 首次读者能快速理解项目用途和主要能力。
- 最短成功路径完整且命令顺序明确。
- 每个章节都服务真实读者任务，没有为了模板而存在的内容。
- 深入内容被压缩或链接到合适文档。

### 成品隔离

- 过程泄漏、无源否定、无端假设、预防式前提、无端转折、防御性辩解为零。
- 没有解释读者未曾期待的缺失文件、功能、依赖或技术选择。
- README 能在脱离所有写作背景后独立成立。

### 写后审计

- 成稿已被读回，功能异常句已改正。
- 标为删除或改写的句子，文件里已经是改后文本。
- README 里没有读回过程、修改说明或作者备忘。

### 原有质量要求

- README 结构清晰、信息充分、吸引人且易读。
- 内容简洁直接，emoji 使用克制。
- 使用 GFM；admonition 仅用于有明确价值的提示。
- 不重复 `LICENSE`、`CONTRIBUTING`、`CHANGELOG` 等专用文件职责。
- 真实且合适的 logo/icon 已在页头使用；不存在合适资源时不虚构。
